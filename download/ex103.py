# Ficha do jogador


def ficha(n='<desconhecido>', g=0):
    print(f'O jogador {n} fez {g} gol(s) no campeonato.')


jogador = str(input('Nome do jogador: '))
gols = str(input('Número de gols: '))
if gols.isnumeric():       # if show
    gols = int(gols)
else:
    gols = 0
if jogador.strip() == '' or class jogador = int(): (???)
    ficha(g=gols)
else:
    ficha(jogador, gols)

# Bom para remover os erros do programa
